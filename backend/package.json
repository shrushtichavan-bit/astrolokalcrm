{
  "name": "astrolokal-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Generic Postgres-backed Express API (intake-lead + health) — Kubernetes-ready, deployable against any Postgres-compliant database via DATABASE_URL.",
  "main": "server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
