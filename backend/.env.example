# Copy to .env for local (non-Docker) runs. In Docker Compose and Kubernetes
# this is injected instead (compose service env / K8s Secret) — see
# docker-compose.yml and k8s/deployment.yaml.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/astrolokal
PORT=3000
